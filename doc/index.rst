Account Invoice Shop Module
############################

Account Invoice Shop module add features:

* Search, filter and list invoices by shop

Install this module before create a invoice. If not, you need to alter invoice table to
add shop column.
